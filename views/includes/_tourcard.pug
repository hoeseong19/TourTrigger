div(data-aos="fade-up")  
  a(href=`/tours/${tour._id}`)
    div(class="card tour my-3") 
      div(class="card-body")
        small= tour.category
        h5= tour.title
        small made by !{tour.guide}
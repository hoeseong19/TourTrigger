extends ../layout

block content
  .container.my-5
    each tour in tours
      a(href=`/tours/${tour._id}`)
        div(class="card tour") 
          h5(class="card-header")= tour.title
          div(class="card-body") 
            p(class="card-text")= tour.description